extends ../../layout/adminHeader
include ../../nav/adminTopbar
block content
  link(rel="stylesheet" href="/admin/css/program.css")
  include ../../nav/adminSidebar
  .container
    form(action="/admin/program/edit" method="post")
      input(name = "_id" hidden value="#{program[0]._id}")
      | 类别：
      input(name="category" class="form-control" value="#{ program[0].category }" required="required")
      | 标题：
      input(name="title" class="form-control" value="#{ program[0].title }" required="required")
      div
        | 正文：
        textarea(name="content" id="editor" required="required" style="width: 100%;height: 450px")= program[0].content
      button(class="btn btn-success" type="submit") 修改
  script.
    var ue = UE.getEditor('editor');
